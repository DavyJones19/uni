

  <button
            type="button"
            className="rounded bg-sky-600 px-3 py-2 text-xs font-semibold text-white shadow hover:bg-sky-700"
            onClick={() => setModalAbierto(true)}
          >
		  
		    {modalAbierto && (
          <div className="absolute inset-0 z-20 flex items-center justify-center bg-black/40 p-4">
            <div className="w-full max-w-sm rounded-lg bg-white p-4 shadow-lg">
              <h2 className="text-sm font-semibold text-zinc-800">¿Qué deseas crear?</h2>
              <div className="mt-4 flex gap-2">
                <button
                  type="button"
                  className="flex-1 rounded border border-zinc-200 bg-zinc-50 px-3 py-2 text-xs font-semibold text-zinc-700 hover:bg-zinc-100"
                  onClick={() => {
                    setModoCreacion('radio');
                    setModalAbierto(false);
                  }}
                >
                  Radio
                </button>
                <button
                  type="button"
                  className="flex-1 rounded border border-zinc-200 bg-zinc-50 px-3 py-2 text-xs font-semibold text-zinc-700 hover:bg-zinc-100"
                  onClick={() => {
                    setPuntosPoligono([]);
                    setModoCreacion('poligono');
                    setModalAbierto(false);
                  }}
                >
                  Poligono
                </button>
              </div>
              <button
                type="button"
                className="mt-3 w-full rounded bg-zinc-100 px-3 py-2 text-xs font-semibold text-zinc-600 hover:bg-zinc-200"
                onClick={() => setModalAbierto(false)}
              >
                Cancelar
              </button>
            </div>
          </div>
        )}
		
		
		
		const [modalAbierto, setModalAbierto] = useState(false);